import { LoadingButton } from "@mui/lab";
import React from "react";

const DeletePriceButton = () => {
  return (
    <LoadingButton variant="outlined" color="error">
      delete
    </LoadingButton>
  );
};

export default DeletePriceButton;
